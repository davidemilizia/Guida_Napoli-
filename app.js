let datiOriginali=[];fetch('dati.json').then(r=>r.json()).then(dati=>{datiOriginali=dati;mostra(dati);document.getElementById('search').addEventListener('input',cerca);});function cerca(){const testo=document.getElementById('search').value.toLowerCase();const filtrati=datiOriginali.filter(x=>(x.Nome||'').toLowerCase().includes(testo));mostra(filtrati);}function mostra(lista){const contenitore=document.getElementById('contenitore');contenitore.innerHTML='';lista.forEach(luogo=>{contenitore.innerHTML+=`<div class="card"><h3>${luogo.Nome||''}</h3><p>${luogo.Descrizione||''}</p><strong>${luogo.Categoria||''}</strong></div>`;});}
